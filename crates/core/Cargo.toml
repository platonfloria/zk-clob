[package]
name = "zk-clob-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
sha2 = "0.11.0"

[dev-dependencies]
zk-clob-test-utils = { path = "../test-utils" }
